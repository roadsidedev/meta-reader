# =============================================================================
# Server-side secrets — NEVER use VITE_ prefix for API keys.
# VITE_ variables are compiled into the browser bundle and publicly visible.
# =============================================================================

# ElevenLabs API key for text-to-speech (used by the /api/tts/synthesize proxy)
ELEVENLABS_API_KEY=

# Claude API key for scene analysis (used by the /api/scene/analyze proxy)
CLAUDE_API_KEY=

# =============================================================================
# Optional configuration
# =============================================================================

# Comma-separated list of allowed CORS origins (defaults to * if unset)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=

# Express server port (defaults to 3000)
PORT=3000
